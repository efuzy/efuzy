import { boot } from 'efuzy/wrappers'

// "async" is optional;
// more info on params: https://efuzy.dev/efuzy-cli/cli-documentation/boot-files#Anatomy-of-a-boot-file
export default boot(async (/* { app, router, Vue ... } */) => {
  // something to do
})
